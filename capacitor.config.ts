import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppIniciodesesion_api',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
